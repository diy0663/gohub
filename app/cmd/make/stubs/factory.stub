package factories

func Make{{StructNamePlural}}(count int64)[]{{PackageName}}.{{StructName}}{
    var objs []{{PackageName}}.{{StructName}}

    // 设置唯一性，如 {{StructName}} 模型的某个字段需要唯一 就得取消本注释 
    // faker.SetGenerateUniqueValues(true)

    for i:=0;i<=count;i++{
         {{VariableName}}Model := {{PackageName}}.{{StructName}}{
            // 使用facker 包去构造大部分字段的数据
            FIXME()
        }
        objs =append(objs,{{VariableName}}Model)
    }
    return objs
}